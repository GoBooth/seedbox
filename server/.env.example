# Required: Replicate API token
REPLICATE_API_TOKEN=your_replicate_token_here

# Required: xAI (Grok) API key for prompt enhancement
XAI_API_KEY=your_grok_api_key_here

# Optional: override the Grok model identifier (defaults to grok-4-fast)
# GROK_MODEL=grok-4-fast
# GROK_API_BASE_URL=https://api.x.ai/v1/chat/completions

# Optional: override the Seedream-4 model identifier (defaults to the latest release)
# You can use a fully-qualified version hash or the plain slug "bytedance/seedream-4"
# Example version hash: bytedance/seedream-4:1234567890abcdef...
# SEEDREAM4_MODEL_VERSION=

# Optional: fal.ai credentials (needed when using the fal provider)
# FAL_KEY=your_fal_api_key
# FAL_MODEL=fal-ai/bytedance/seedream/v4/edit

# Optional: Google Gemini credentials for the Nano Banana provider
# GEMINI_API_KEY=your_gemini_api_key_here
# NANO_BANANA_MODEL=gemini-1.5-flash

# Optional: maximum dimension for reference images (default 2048)
# MAX_IMAGE_DIMENSION=2048

# Optional: override default server port (default 5002)
# PORT=5002

# Optional: comma-separated origins allowed by CORS
# ALLOWED_ORIGINS=http://localhost:5173
